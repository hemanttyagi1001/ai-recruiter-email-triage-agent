"""
Observability — what the agent tells the outside world about its own runs.

Everything in this package is OPTIONAL and OFF by default. Nothing here may
change a routing decision, a draft, or a send; it only describes work that
already happened. If the whole package failed to import, the pipeline would
still be correct — just harder to debug.
"""
